import React, { Component } from 'react';
import classes from './Layout.scss';

class Layout extends Component {
  render() {
    return (
      <main className={classes.Layout}>
          <p>
              Shelter Layout
              ogarnąć nawigację by nawigowała a nie renderowała XD
          </p>
      </main>
    );
  }
}

export default Layout;
